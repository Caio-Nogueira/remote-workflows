{
  "name": "remote-workflows-r2-backup-example",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler types --include-runtime=false --check && tsc --noEmit",
    "types": "wrangler types --include-runtime=false",
    "serve": "bun run src/server.ts"
  },
  "dependencies": {
    "@cloudflare/remote-workflows": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@types/bun": "catalog:",
    "typescript": "catalog:",
    "wrangler": "4.128.0"
  }
}
